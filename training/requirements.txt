# Baked into training/Dockerfile by .github/workflows/build-image.yml.
# TRL API drift has broken GRPO before — pin exact versions.
# NOTE: the extra must exist for this unsloth version or pip silently
# ignores it (cu128-torch280 did not exist — extras top out at torch270).
# Resolves to torch 2.7.0+cu126 via the xformers==0.0.30 pin.
unsloth[cu126-torch270]==2025.6.3
unsloth_zoo==2025.6.2
# transformers >=4.52 GradientCheckpointingLayer breaks unsloth's for_inference()
# (layers keep stale training flags -> IndexError in LlamaModel_fast_forward)
transformers==4.51.3
trl==0.18.1
datasets==3.6.0
huggingface_hub==0.32.4
peft==0.15.2
accelerate==1.7.0
bitsandbytes==0.46.0
slack_sdk>=3.27.0
